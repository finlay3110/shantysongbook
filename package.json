{
  "name": "ucn-songbook",
  "version": "1.0.0",
  "private": true,
  "description": "Fan-made digital songbook for the UCN Bridge Command LARP community",
  "//": "The app itself has NO build step and no runtime dependencies — it is plain HTML/CSS/JS served as-is. This file exists only so the test and validation tooling can be installed.",
  "scripts": {
    "validate": "node tools/validate-songs.mjs",
    "test": "node tools/test-app.mjs",
    "test:headed": "node tools/test-app.mjs --headed",
    "serve": "python3 -m http.server 8899"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
